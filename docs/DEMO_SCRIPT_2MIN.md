# 2分钟演示稿（自动生成）

> 本文件由 `scripts/generate_materials.py --output outputs --docs docs` 生成。截至 2026-04-29，本文数值来自根目录 `outputs/comparison_metrics.json` 的小样例烟测。最终 V4 提交参数与留出集指标见 `SUBMISSION_PARAM_CARD_V4.md`。

大家好，我们的方案面向赛道二3D异构CIM调度优化。

第一，我们把MoE共现热点抽成热子图，做精确分配，先解决最关键冲突。
第二，在全局上采用并行多起点的swap/2-opt局部搜索，并叠加模拟退火，避免陷入局部最优。
第三，引入自适应副本预算与分块量化稀疏压缩，降低传输开销与冲突。
第四，时序仿真中显式建模子立方体异构算力和带宽，指标更贴近真实硬件。

在根 `outputs` 样例中，端到端时延从 34117 cycles 降到 9732 cycles，降低 71.47%。
同时时间利用率提升 245.83%。

我们还输出了完整可解释剖析，包括每次推理时延分布和sub-cube争用热度，便于评审快速判断方案稳定性与可扩展性。
谢谢大家。
