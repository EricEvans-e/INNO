\section{实验设置与结果}
\subsection{设置与评价}
正式主方案使用 \texttt{sample\_model.json} 和由 mock trace 变体构成的平衡轨迹，Cube 配置为 $N=3,D=2,H=W=4096$，最大并行 Sub-Cube 数为 9，并启用严格容量比 2.0。主方案使用 V5 参数卡中记录的重叠、搜索、复制、压缩和放置参数；所有输出由 \texttt{validate\_submission.py} 再次检查。评价指标包括端到端延迟、空间利用率、时间利用率、切换代价、冲突分数、映射率和容量比。

实验输入与协议如表~\ref{tab:setup} 所示。样例模型包含 16 个路由专家、共享算子、路由器、合并算子和输出层；平衡轨迹包含 20 条推理记录，每条记录的 top-$k$ 为 4。模型文件是 int8 元数据描述，主结果使用项目生成的平衡 mock trace，不代表真实芯片或官方激活分布。First Fit 基线与 V5 primary 使用相同模型、轨迹、硬件配置和验证规则，区别仅在于是否启用轨迹感知分组、搜索和自适应复制。

\begin{table*}[t]
\centering
\caption{主实验配置与数据边界。}
\label{tab:setup}
\begin{tabularx}{\textwidth}{l l X}
\toprule
项目 & 设置 & 说明 \\
\midrule
模型 & 16 experts，top-$k=4$，20 条推理，int8 metadata & 包含共享算子、路由器、专家、合并算子和输出层 \\
轨迹 & balanced mock trace & 由项目自定义专家激活分布生成，非官方轨迹 \\
资源 & $N=3,D=2,H=W=4096$，9 个 Sub-Cube 并行 & 同一 Sub-Cube 单一激活，容量比上限为 2.0 \\
基线 & First Fit & 顺序首个可行位置，不启用 MoE 优化 \\
主方案 & V5 primary & Best Fit、轨迹统计、有限复制、局部搜索和静态调度 \\
校验 & 独立 validator & 要求 \texttt{ok=true}、\texttt{errors=[]}、映射率为 1.0 \\
\bottomrule
\end{tabularx}
\end{table*}

\subsection{主方案结果}
表~\ref{tab:primary} 给出 V5 primary 与 First Fit 基线的对比。优化方案将延迟降低 \PrimaryImprovement{}，同时将时间利用率由 0.2355 提升至 \PrimaryTemporal{}，切换代价从 15,576 降至 1,083 cycles。两者映射率均为 1.0。容量比 \PrimaryCapacity{} 小于严格上限 2.0，独立验证报告为 \texttt{ok=true} 且无错误。

\begin{table*}[t]
\centering
\caption{V5 primary 在平衡 mock trace 上的主结果。}
\label{tab:primary}
\begin{tabular}{lrrrrrr}
\toprule
方案 & 延迟(cycles) & 空间利用率 & 时间利用率 & 切换代价 & 冲突分数 & 映射率\\
\midrule
First Fit 基线 & 539,729 & 0.5035 & 0.2355 & 15,576 & 6.0 & 1.0\\
V5 primary & \PrimaryLatency{} & \PrimarySpace{} & \PrimaryTemporal{} & 1,083 & \PrimaryConflict{} & 1.0\\
\bottomrule
\end{tabular}
\end{table*}

除主表指标外，优化方案的 memory utilization 为 0.1998，平均带宽利用率为 0.5395，最大带宽利用率为 0.9707，Sub-Cube busy imbalance 为 0.8523；基线对应值分别为 0.1605、0.2344、0.8210 和 1.4441。优化后切换代价降低 93.05\%，时间利用率提高约 130.62\%，但最大带宽利用率接近 1，说明该配置已经受到局部带宽热点约束，不能简单从空间利用率继续推断性能收益。

\setcounter{figure}{4}
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/figure5.png}
\caption{同一平衡 mock trace 上的延迟对比。V5.1 仅为候选，尚需官方激活轨迹复核。}
\label{fig:latency}
\end{figure*}

\subsection{复制消融与候选比较}
在 V5 primary 的消融中，关闭复制后，优化延迟为 257,743 cycles，较基线降低 52.25\%；完整配置为 \PrimaryLatency{} cycles。这表明在该工作负载和参数设置下，副本预算对减少排队与气泡具有贡献。需要注意，当前 Best Fit 加 replication 消融与完整方案在该样例上得到相同延迟，因此该组结果不能单独证明互斥分组带来的边际收益；后续应补充 Best Fit only、分组 only、副本 only 与完整方案的正交消融。

\begin{table*}[t]
\centering
\caption{V5 primary 的复制消融结果。}
\label{tab:ablation}
\begin{tabular}{lrrrrr}
\toprule
方案 & 延迟(cycles) & 延迟降低 & 空间利用率 & 时间利用率 & 切换代价\\
\midrule
First Fit 基线 & 539,729 & 0.00\% & 0.5035 & 0.2355 & 15,576\\
关闭复制 & 257,743 & 52.25\% & 0.5035 & 0.4934 & 9,336\\
V5 primary & \PrimaryLatency{} & \PrimaryImprovement{} & \PrimarySpace{} & \PrimaryTemporal{} & 1,083\\
\bottomrule
\end{tabular}
\end{table*}

V5.1 将专家副本预算提高至 0.35，同时关闭共享算子复制。它得到 \CandidateLatency{} cycles（\CandidateImprovement{}），较 V5 primary 低 2,764 cycles，但空间利用率升至 \CandidateSpace{}，冲突分数升至 \CandidateConflict{}。因此本文将其表述为 \candidate{}，不替代正式 V5 primary。

\begin{table*}[t]
\centering
\caption{正式方案与增强候选方案的对比。}
\label{tab:candidate}
\begin{tabular}{lrrrrl}
\toprule
方案 & 延迟(cycles) & 延迟降低 & 空间利用率 & 冲突分数 & 状态\\
\midrule
First Fit 基线 & 539,729 & 0.00\% & 0.5035 & 6.0 & 基线\\
V5 primary & \PrimaryLatency{} & \PrimaryImprovement{} & \PrimarySpace{} & \PrimaryConflict{} & 正式推荐\\
V5.1 & \CandidateLatency{} & \CandidateImprovement{} & \CandidateSpace{} & \CandidateConflict{} & 候选，待官方轨迹复核\\
\bottomrule
\end{tabular}
\end{table*}

\subsection{大规模 metadata-only 验证}
为验证工程链路在较多专家下仍能运行，使用脚本生成 \LargeExperts{} 专家、256 条推理、top-$k=4$ 的 metadata-only MoE 模型与轨迹，并采用 $N=3,D=8,H=W=4096$。优化后延迟由 175,720 降至 \LargeLatency{} cycles，降低 \LargeImprovement{}，映射率为 1.0，容量比为 1.9427。该实验只证明 parser、映射、调度与验证器在更大专家数量下可运行；它不等价于真实 DeepSeek-671B 权重部署或官方轨迹性能。

\subsection{局限性与可复现性}
截至本文完成时，正式评测激活轨迹尚未在本地项目中完成回归，因而所有性能数字受自定义 mock 与 synthetic 轨迹分布约束。部分硬件惩罚参数为调度模型假设，未由芯片实测校准。ONNX 支持是元数据级轻量适配，未覆盖完整执行语义。尽管存在这些边界，项目保存了输入哈希、配置、映射、调度、内部 IR、可视化和验证报告；在当前代码版本上，项目虚拟环境回归测试记录为 \TestCount{} passed。

复现时应使用项目虚拟环境和参数卡中的完整命令，先运行 \texttt{pytest}，再生成或读取指定输出目录，最后执行独立的 \texttt{validate\_submission.py}。主方案的证据链包括提交文件、运行清单、指标文件和独立验证报告，其中映射文件为 \path{solution.json}，独立校验报告为 \path{validation_report.json}。验证器报告必须为 \texttt{ok=true} 且 \texttt{errors=[]}。若替换为官方轨迹，应保持硬件配置和验证规则不变，重新生成轨迹哈希、映射、调度、指标和报告，并将新结果单独标为 official-trace replay，不能覆盖本文的 mock trace 数字。
